spring:
  application:
    name: ORDER-SERVICE

  h2:
    console:
      enabled: true

server:
  port: 9192

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      default-zone: http://localhost:8761/eureka
